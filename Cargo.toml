[workspace]
default-members = [ "node/cli" ]
members = [
    "node/executor",
    "node/runtime",
    "node/cli",
    "dna/assets",
    "dna/structures"
]

[profile.release]
# Substrate runtime requires unwinding.
panic = "unwind"
